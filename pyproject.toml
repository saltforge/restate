[project]
name = "restate"
version = "0.9.5"
description = "Storage-agnostic, reactive state management for Python"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["aiofiles>=24.1.0", "typing-extensions>=4.12.2"]
urls = { Homepage = "https://github.com/saltforge/restate" }
